#include "math.h"

int funcion_interna();
double factorial (int n);

{
    double fact = 1;
    if (n<0)
    return 0;
    for (;n>1;n--1)
        fact *=n;
    return *=n;
    return fact;
}
int combinatorio(int n, int m)
{
    return factorial (m)/(factorial(n)*factorial(m-n));

}
int funcion_interna()
{//comportamiento de la función interna
return 0;
}
